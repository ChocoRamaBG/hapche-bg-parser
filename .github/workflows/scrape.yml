name: Run Scraper 🚀

on:
  workflow_dispatch: # Ръчно пускане за истински hustlers
  # schedule:        # Ако искаш да се пуска сам
  #   - cron: '0 */6 * * *' 

jobs:
  scrape-job:
    runs-on: ubuntu-latest
    
    # ⚠️ ВАЖНО: Даваме права на скрипта да пише в репото!
    permissions:
      contents: write
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10' # 3.9 is so 2020

      - name: Install dependencies
        run: |
          pip install webdriver-manager pandas selenium openpyxl

      - name: Run Scraper Script (Grind Mode)
        run: python main.py

      # 💾 КАЧВАМЕ ДАННИТЕ
      - name: Upload Data Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Hapche-Data-Part-${{ github.run_id }}
          path: scraped_data/

      # 🧠 ЗАПАЗВАМЕ ПРОГРЕСА В РЕПОТО (COMMIT BACK)
      - name: Commit Progress (Auto-Save)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 Save Game: Updated scraper progress [skip ci]"
          file_pattern: "last_page.txt"
